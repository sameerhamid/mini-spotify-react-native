import React from 'react';

import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
} from 'react-native';

function App(): React.JSX.Element {
  return (
    <SafeAreaView>
      <StatusBar />

      <Text>Testing</Text>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({});

export default App;
